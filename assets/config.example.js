/* Rent 2 Go — Supabase config TEMPLATE
 * 1. Copy this file to `config.js` (config.js is git-ignored — keys never commit)
 * 2. Paste your dedicated Rent 2 Go Supabase project's URL + anon public key
 *    (Supabase Dashboard → Settings → API)
 */
window.R2G_CONFIG = {
  SUPABASE_URL: 'https://YOUR-PROJECT.supabase.co',
  SUPABASE_ANON_KEY: 'YOUR-ANON-PUBLIC-KEY',
};
